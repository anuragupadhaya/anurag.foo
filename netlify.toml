# netlify.toml

[build]
  command = "hugo --gc --minify -b $URL"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.119.0"
  NODE_ENV = "production"
  TZ = "UTC+05:30"

[context.production.environment]
  HUGO_ENV = "production"
